<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>//无ActionBar
        <item name="windowNoTitle">true</item> //无标题
        <item name="android:windowFullscreen">true</item> //全屏


    </style>

    <style name="tallerBarStyle" parent="@android:style/Widget.SeekBar" >
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_scl</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">10dip</item>
    </style>

    <style name="vod_player_menu_anim" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/vod_control_menu_right_in</item>
        <item name="android:windowExitAnimation">@anim/vod_control_menu_right_out</item>
    </style>

    <style name="vod_player_menu_anim1" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/vod_control_bar_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/vod_control_bar_bottom_out</item>
    </style>


</resources>
